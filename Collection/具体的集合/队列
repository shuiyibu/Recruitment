# 队列与双端队列


>**```java.util.Queue<E>```**

| |**Throws exception**|**Throws exception**|
| -------------: |:-------------:| :-----:|
|**Insert**|add(e)|offer(e)|
|**Remove**|remove()|poll()|
|**Examine**|element()|peek()|

# 优先级队列
>优先级队列的元素可以按照任意的顺序插入，却总是按照排序的顺序进行检索。
也就是无论何时调用`remove`方法，总会获得当前优先队列中最小的元素。

- 数据元素：**堆（heap）**
- 对象比较
